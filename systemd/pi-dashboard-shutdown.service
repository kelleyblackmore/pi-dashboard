[Unit]
Description=Pi Dashboard Graceful Shutdown Handler
DefaultDependencies=no
Before=shutdown.target reboot.target halt.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/pi-dashboard-shutdown
TimeoutStartSec=30
RemainAfterExit=yes

[Install]
WantedBy=halt.target reboot.target shutdown.target
